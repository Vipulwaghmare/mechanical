import React from 'react';
import Base from '../Base';

const GateBase= ({children}) => {
    return(
        <Base>
            Gate Base
        {children}
        </Base>
    )
}

export default GateBase