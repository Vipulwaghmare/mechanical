import React from 'react';
import Base from '../Base';

const EseBase = ({children}) => {
    return(
        <Base>
            EseBase
        {children}
        </Base>
    )
}

export default EseBase;